// Configuración global para Vitest. Las pruebas actuales cubren utilidades
// puras (parsing de enlaces, detección de duplicados, validaciones), por lo
// que no se requieren extensiones adicionales de DOM matchers por ahora.
export {};
